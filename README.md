# UE_Learning

Учебный проект на Unreal Engine 5.7 для выполнения домашних работ по курсу Unreal Engine Game Developer.

---

## Домашняя работа 1

Создан C++ проект Unreal Engine и размещён в Git-репозитории.

Реализован автоматический тест `UE_Learning.PlayerControllerDefaults`, который проверяет валидность CDO пользовательского `AUE_LearningPlayerController`.

Создан плагин `HomeworkPlugin`, который при запуске игрового мира создаёт визуальный объект перед игроком.

После замечаний преподавателя плагин был доработан:

- `AUE_LearningGameMode` и `AUE_LearningPlayerController` связаны в C++ коде;
- визуальный объект вынесен в отдельный Blueprint-ассет `BP_HomeworkCube`;
- плагин спавнит готовый Actor через `SpawnActor`;
- лишние вызовы настройки масштаба и пробуждения физики удалены.

---

## Домашняя работа 2

Создана базовая C++ архитектура персонажа:

- `TPBaseCharacter` — базовый класс персонажа;
- `TPPlayerCharacter` — класс игрока на основе базового персонажа;
- `TPAttributeSet` — заготовка под атрибуты Gameplay Ability System;
- `Interactable` — интерфейс взаимодействия;
- `TPGA_Dash` — тестовая Gameplay Ability для рывка.

Настроено управление через Enhanced Input:

- движение;
- обзор мышью;
- прыжок;
- приседание;
- взаимодействие;
- рывок.

Персонаж подключён через Blueprint-наследник `BP_TPPlayerCharacter`.

После замечания преподавателя часть числовых параметров вынесена из кода в именованные `UPROPERTY`:

- длина камеры;
- дистанция взаимодействия;
- расстояние спавна тестовых акторов;
- высота спавна тестовых акторов;
- скорость поворота персонажа;
- скорости движения.

Это убирает магические числа из основной логики и позволяет настраивать параметры через Blueprint.

---

## Домашняя работа 3

Добавлен персонаж `Alien Soldier` с набором дополнительных анимаций.

Настроена анимационная система:

- `ABP_AlienSoldier`;
- Blend Space для обычного движения;
- Blend Space для движения в приседе;
- ходьба;
- бег;
- спринт;
- прыжок со стартом, фазой полёта и приземлением;
- прицеливание стоя;
- прицеливание в приседе;
- IK для левой руки при удержании оружия.

Добавлено оружие персонажа:

- C++ класс `TPWeaponActor`;
- Blueprint оружия `BP_RifleWeapon`;
- оружие задаётся через настройку `Weapon Class` в `BP_TPPlayerCharacter`;
- оружие крепится к сокету на скелете персонажа;
- левая рука подтягивается к IK-точке на оружии.

Создан отдельный модуль `HW3Module`, содержащий тестовый C++ Actor.

Создан отдельный плагин `HW3Plugin`, содержащий тестовый C++ Actor.

В классе персонажа добавлены функции для создания тестовых акторов из модуля и плагина перед игроком. Для вызова этих функций настроены отдельные клавиши через Enhanced Input.

После замечаний преподавателя была доработана архитектура персонажа:

- Blueprint-геттеры состояния персонажа вынесены в публичный интерфейс;
- геттеры состояния сделаны `BlueprintPure`;
- независимые булевые флаги движения заменены на `ETPMovementState`;
- режимы `Running`, `Walking`, `Sprinting`, `Aiming` представлены через `enum class`;
- скорость и режим поворота обновляются централизованно через `SetMovementState`.

Для анимаций добавлен C++ класс `TPCharacterAnimInstance`.

В него перенесены анимационные производные данные:

- скорость движения;
- направление движения;
- состояние падения;
- состояние приседа;
- подготовка к приземлению.

Предсказание приземления перенесено из `TPPlayerCharacter` в `TPCharacterAnimInstance`.

Подготовка к приземлению теперь рассчитывается не по фиксированной дистанции до земли, а по прогнозируемому времени до удара:

- вниз выполняется trace для определения расстояния до земли;
- учитываются текущая вертикальная скорость и гравитация;
- вычисляется `TimeToImpact`;
- anticipation pose включается, когда `TimeToImpact` меньше заданного `LandingLeadTime`.

Так момент включения landing-анимации становится стабильнее при разных скоростях падения и разных высотах.

---

## Домашняя работа 4

В рамках работы использована уже созданная иерархия персонажей:

- `TPBaseCharacter` — базовый C++ класс персонажа;
- `TPPlayerCharacter` — основной игровой персонаж на основе базового класса;
- управление реализовано через Enhanced Input;
- персонажу подключены кастомные анимации Alien Soldier через Animation Blueprint, Blend Space и State Machine.

Расширена система взаимодействия с объектами сцены.

Интерфейс `Interactable` дополнен методами:

- `Interact` — выполнить взаимодействие;
- `CanInteract` — проверить возможность взаимодействия;
- `GetInteractionPrompt` — получить текст подсказки;
- `OnFocused` — реакция на наведение / фокус;
- `OnUnfocused` — реакция на потерю фокуса.

Создан базовый класс интерактивного объекта `InteractableActor`, реализующий общую логику взаимодействия, подсказки и фокусировки.

Добавлены 3 категории интерактивных объектов:

- `PickupInteractableActor` — предмет для подбора;
- `ActivatorInteractableActor` — переключатель, управляющий другим Actor;
- `InfoInteractableActor` — информационный объект.

Добавлен `InteractionDetectorComponent`, который:

- ищет интерактивные объекты в радиусе игрока;
- выбирает ближайший доступный объект;
- вызывает `OnFocused` / `OnUnfocused`;
- обновляет текущий объект взаимодействия;
- обновляет подсказку после изменения состояния объекта.

Для визуального выделения интерактивных объектов реализован outline через `CustomDepth / CustomStencil` и post-process material.

С помощью CommonUI реализована экранная подсказка взаимодействия:

- создан `InteractionPromptWidget` на основе `UCommonUserWidget`;
- создан Blueprint-виджет `WBP_InteractionPrompt`;
- текст подсказки берётся из `GetInteractionPrompt`;
- подсказка появляется при наведении на интерактивный объект;
- подсказка скрывается при потере фокуса;
- текст обновляется динамически после взаимодействия, например `Show info` / `Hide info`.

Дополнительно старый тестовый объект взаимодействия из ДЗ 2 адаптирован под новую систему взаимодействия.

Для этого создан Blueprint-наследник `BP_TestInteractable_HW2` от базового класса `InteractableActor`. Объект работает через общую систему `InteractionDetectorComponent`, получает outline при фокусе и отображает CommonUI-подсказку.

При взаимодействии с объектом над ним появляется надпись:

`Object from homework 2`

Надпись реализована через `TextRenderComponent` внутри Blueprint и скрывается через несколько секунд после взаимодействия.

Система ввода доработана: вместо отдельных `UInputAction*` в персонаже используется `TPInputConfig` Data Asset с привязкой действий через `GameplayTag`. Это позволяет менять Input Action assets без добавления новых полей в `TPPlayerCharacter`.

CommonUI-подсказка взаимодействия использует `UCommonTextBlock`. Клавиша взаимодействия отображается динамически: она берётся из `InputConfig` и `DefaultMappingContext`, поэтому при изменении клавиши в `IMC_Default` текст подсказки обновляется без правки C++.

---

## Домашняя работа 5

В рамках домашней работы продолжена разработка общей системы NPC и квестовых сценариев. Задача выполнена не как отдельный изолированный пример, а как развитие уже существующей архитектуры проекта: базовых персонажей, системы взаимодействия, AI, оружия, UI и Gameplay Ability System.

### Базовая архитектура NPC

Для NPC используется общая иерархия персонажей:

- `TPBaseCharacter` — общий базовый класс персонажа;
- `TPNPCCharacter` — базовый класс NPC;
- `TPPlayerCharacter` — игровой персонаж;
- `TPNPCAIController` — AI Controller для NPC;
- `TPNPCDefinition` — Data Asset с параметрами конкретного NPC;
- `TPNPCTypes` — общие типы и перечисления для NPC.

Созданы два типа NPC:

- `BP_NPC_Bandit` — враждебный NPC;
- `BP_NPC_Researcher` — нейтральный NPC-квестодатель.

Параметры конкретных NPC вынесены в Data Asset:

- `DA_NPC_Bandit`;
- `DA_NPC_Researcher`.

Через Data Asset задаются принадлежность к команде, параметры движения, возможность взаимодействия, текст подсказки, оружие по умолчанию и другие настройки. Это позволяет создавать новые разновидности NPC без дублирования основной C++-логики.

### Поведение враждебного NPC

Для разбойника используется State Tree:

- `ST_NPC_Bandit`.

В поведении враждебного NPC реализованы состояния:

- патрулирование по случайным точкам NavMesh;
- ожидание между точками патруля;
- обнаружение игрока;
- преследование цели;
- остановка на дистанции атаки;
- стрельба по цели.

Для State Tree добавлены переиспользуемые C++-задачи и вычисляемые данные:

- поиск случайной точки патрулирования;
- переключение скорости NPC;
- получение текущей цели;
- вычисление дистанции до цели;
- проверка дистанции стрельбы;
- проверка прямой видимости;
- выполнение одиночного выстрела через оружие NPC.

Обнаружение игрока реализовано через `AI Perception` в `TPNPCAIController`. NPC запоминает цель при кратковременной потере видимости и возвращается к патрулированию после заданной задержки забывания цели.

### Поведение нейтрального NPC

Нейтральный NPC `BP_NPC_Researcher` используется как квестодатель.

Для него добавлен компонент:

- `TPQuestGiverComponent`.

Компонент отвечает за:

- обработку взаимодействия с NPC;
- открытие окна предложения задания;
- запуск цели после принятия задания;
- обновление подсказки взаимодействия;
- обработку возврата игрока с артефактом;
- завершение задания после сдачи результата.

Логика выдачи задания вынесена из Level Blueprint в переиспользуемый компонент. Это снижает связанность сцены и позволяет использовать тот же подход для других NPC-квестодателей.

### UI предложения задания

Для окна предложения задания используется виджет:

- `TPQuestOfferWidget`;
- `WBP_QuestOffer`.

Виджет применяется в двух сценариях:

- предложение задания от нейтрального NPC;
- переговоры с вражеским NPC.

В окне поддерживаются:

- заголовок;
- описание;
- кнопка принятия;
- кнопка отказа;
- закрытие через `Esc`;
- подтверждение через UI-кнопку;
- постановка игры на паузу на время диалога.

### Система целей

Для управления целью используется Actor:

- `TPLevelObjectiveManager`.

В рамках домашней работы он расширен до сценарной логики с квестовым предметом.

Добавлены режимы завершения цели:

- уничтожить всех целевых NPC;
- получить квестовый предмет;
- уничтожить цели и получить квестовый предмет.

Для текущего сценария используется получение предмета с последующим возвратом к квестодателю. Это позволяет отделить сам факт получения артефакта от финальной сдачи задания.

`TPLevelObjectiveManager` также выбирает одного случайного NPC из списка целей, который может выдать квестовый предмет при смерти. Благодаря этому при наличии нескольких разбойников артефакт появляется только один раз.

### Квестовый предмет

Добавлен квестовый предмет:

- `TPQuestItemActor`;
- `BP_QuestArtifact`.

Предмет реализован через существующую систему взаимодействия `Interactable`. Он имеет идентификатор:

- `BanditArtifact`.

При подборе предмет сообщает `TPLevelObjectiveManager`, что нужный артефакт получен.

Для выпадения предмета добавлен компонент:

- `TPQuestItemDropComponent`.

Компонент вешается на NPC и подписывается на событие смерти персонажа. После смерти выбранного NPC он создаёт `BP_QuestArtifact` рядом с телом. Предмет использует физику и корректно взаимодействует с поверхностью уровня.

### Переговоры с вражеским NPC

Для альтернативного сценария добавлен компонент:

- `TPEnemyNegotiationComponent`.

Компонент размещается на `BP_NPC_Bandit` и обрабатывает взаимодействие с вражеским NPC во время активного задания.

Сценарий переговоров:

1. игрок принимает задание у `BP_NPC_Researcher`;
2. подходит к `BP_NPC_Bandit`;
3. открывает окно переговоров;
4. выбирает вариант получения артефакта;
5. артефакт засчитывается без убийства NPC;
6. игрок возвращается к `BP_NPC_Researcher`;
7. сдаёт задание.

Таким образом, артефакт можно получить двумя способами:

- через сражение и подбор предмета после смерти разбойника;
- через переговоры с вражеским NPC.

### Завершение задания

После получения артефакта задание не завершается автоматически. Игрок должен вернуться к нейтральному NPC и сдать предмет.

Для этого в `TPQuestGiverComponent` добавлен отдельный prompt состояния возврата:

- `TurnInPrompt`.

После сдачи задания вызывается завершение цели и отображается экран завершения. Такой сценарий лучше соответствует структуре квеста: получение задания, выполнение условия, возвращение к квестодателю и получение награды.

### Итоговое состояние

В рамках домашней работы реализованы требования:

- созданы два типа NPC: враждебный и нейтральный;
- для враждебного NPC задано поведение через State Tree и AI Perception;
- нейтральный NPC используется как квестодатель;
- система квестов описывает сценарий взаимодействия с нейтральным NPC;
- реализован сценарий получения артефакта через бой;
- реализован альтернативный сценарий получения артефакта через переговоры с вражеским NPC;
- после получения артефакта игрок возвращается к квестодателю и завершает задание;
- логика вынесена в переиспользуемые C++ компоненты, а Blueprint используется для настройки ассетов, UI и объектов уровня.

---

## Домашняя работа 6

В рамках домашней работы добавлен отдельный уровень окружения для проверки систем Landscape, Foliage и постпроцессов. Уровень не заменяет существующую карту с персонажем, NPC, оружием и квестовой логикой, а расширяет проект отдельной сценой, на которой можно развивать визуальное окружение и проверять настройки рельефа, растительности и финальной обработки изображения.

### Уровень окружения

Создана отдельная карта:

* `Lvl_HW6_Landscape`.

Карта размещена в папке:

* `Content/Homework/HW6/Maps`.

Для уровня создан Landscape с базовым рельефом: центральной игровой зоной, холмами, низинами, дорожкой и каменистыми участками. Это отделяет окружение от обычных Static Mesh-плоскостей и позволяет использовать специализированную систему рельефа Unreal Engine.

Для поверхности Landscape добавлен материал:

* `M_HW6_Landscape`.

Материал использует Landscape Layers:

* `Grass`;
* `Dirt`;
* `Rock`.

### Система окружения через Foliage

Для наполнения карты использована система Foliage.

Созданы Foliage Type assets:

* `FT_HW6_Grass`;
* `FT_HW6_Bush`;
* `FT_HW6_Rock`.

Через Foliage на Landscape размещены трава, кусты и камни. Объекты распределены с учётом структуры уровня: центральная зона остаётся читаемой для игрока, растительность размещается группами, а камни используются преимущественно на склонах и каменистых участках.

Подготовленные ассеты окружения размещены в структуре:

* `Content/Homework/HW6`.

Так сцена окружения остаётся изолированной по контенту, но сохраняется внутри общего проекта и может быть использована как основа для дальнейшего развития открытой карты, боевой зоны или квестовой локации.

### Постпроцесс

Для визуального изменения сцены добавлен глобальный Post Process Volume.

Создан постпроцесс-материал:

* `M_HW6_PP_NatureTone`.

Материал использует домен `Post Process`, получает изображение сцены через `SceneTexture: PostProcessInput0` и выводит результат через `Emissive Color`. Эффект добавляет мягкое природное тонирование сцены без жёсткого искажения освещения.

`PostProcessVolume` настроен как глобальный для уровня через `Infinite Extent (Unbound)`. Дополнительно используется виньетка, чтобы визуальное изменение карты было заметно при запуске уровня.


### Процедурное окружение через PCG

В качестве дополнительного слоя окружения добавлен PCG Graph:

* `PCG_HW6_RockSlope`.

Граф используется на карте `Lvl_HW6_Landscape` через `PCGVolume_HW6_RockSlope`. PCG получает данные Landscape, создаёт точки на поверхности внутри заданного объёма, применяет случайный масштаб и поворот, после чего размещает подготовленные Static Mesh окружения.

Такой подход отделяет ручную художественную расстановку Foliage от процедурного наполнения повторяемыми объектами. Ручной Foliage остаётся для контролируемой композиции уровня, а PCG может использоваться как расширяемая основа для генерации каменистых склонов, лесных зон и других участков окружения.


### Итоговое состояние

В рамках домашней работы реализованы требования:

* создан отдельный уровень на основе Landscape;
* для Landscape настроен материал со слоями поверхности;
* на Landscape размещены объекты окружения через Foliage;
* добавлен глобальный Post Process Volume;
* создан Post Process Material, изменяющий отображение карты;
* добавлен PCG Graph для процедурного размещения объектов окружения на Landscape;
* сцена окружения вынесена в отдельную структуру `Content/Homework/HW6` и не нарушает существующую архитектуру персонажа, NPC, оружия, UI и квестовой системы.
---

## Домашняя работа 7

Начата работа над системой звука и визуальных эффектов. На первом промежуточном этапе добавлена звуковая часть без Niagara VFX, чтобы отдельно зафиксировать изменения в архитектуре аудио.

### Звуковая архитектура персонажей

Добавлен компонент `TPCharacterAudioComponent`, который отвечает за звуки, связанные с персонажем:

- воспроизведение шагов левой и правой ноги;
- автоматическое определение момента шага через трассировку от костей стоп;
- воспроизведение сердцебиения при низком уровне здоровья игрока.

Компонент подключён к базовой иерархии персонажей, поэтому его можно использовать как для игрока, так и для NPC без дублирования логики.

Компонент проверяет положение стоп относительно поверхности и запускает звук при посадке ноги на землю. Это позволяет привязать звук к фактическому контакту стопы с поверхностью, включая движение по наклонным участкам и приземление.

### Звуки оружия и попаданий

В `TPWeaponActor` добавлены параметры звука выстрела. Звук воспроизводится после успешного создания projectile, поэтому не запускается при невозможности выстрела.

В `TPBlasterProjectile` добавлены звуки попадания:

- звук попадания в тело;
- звук попадания в голову.

Для определения попадания в голову используется проверка расстояния от точки попадания до socket / bone головы на Skeletal Mesh персонажа. Это заменяет приблизительную проверку по высоте капсулы и позволяет точнее выбирать звук попадания.

### Звуки смерти и окружения

В `TPNPCCharacter` добавлен звук падения мёртвого тела, который вызывается при переходе NPC в состояние смерти.

На уровне добавлен ambient-звук окружения. Он отделён от позиционных звуков персонажей и оружия и используется как общий фон сцены.

### Добавленные ассеты

Для настройки аудио добавлены SoundWave и Attenuation assets в `Content/Audio`:

- звук выстрела;
- звук попадания в тело;
- звук попадания в голову;
- шаг левой ноги;
- шаг правой ноги;
- фоновый ambient;
- звук падения тела;
- сердцебиение при низком здоровье;
- attenuation-настройки для 3D-звуков персонажа и оружия.

### Niagara VFX

Визуальные эффекты перенесены на Niagara.
Добавлены Niagara System assets:

- `NS_HW7_MuzzleFlash` — короткая красная энергетическая вспышка у точки `MuzzlePoint` при выстреле;
- `NS_HW7_ImpactSpark` — эффект попадания projectile в окружение;
- `NS_HW7_NPCImpactBurst` — отдельный мягкий эффект попадания по NPC.

Для Niagara-спрайтов создан общий материал `M_HW7_VFX_SoftSprite`. Он использует `Additive`, `Unlit` и радиальный градиент, чтобы эффекты не выглядели как жёсткие плоские спрайты.

VFX подключены через уже существующие события игровой архитектуры:

- `OnWeaponFired` в `TPWeaponActor` запускает `NS_HW7_MuzzleFlash`;
- `OnProjectileImpact` в `TPBlasterProjectile` выбирает эффект по типу цели:
  - при попадании по NPC запускается `NS_HW7_NPCImpactBurst`;
  - при попадании по окружению запускается `NS_HW7_ImpactSpark`.

Так VFX-система остаётся разделённой по ответственности: C++ отдаёт игровые события, Blueprint выбирает визуальный эффект, а Niagara отвечает за внешний вид.
---

## Домашняя работа 8

В рамках домашней работы продолжена разработка квестовой системы, добавленной в предыдущих заданиях.

В систему квестов добавлена поддержка локализации через стандартные механизмы Unreal Engine.

Текстовые данные квестов переведены на `FText` с локализуемыми ключами `NSLOCTEXT`.

Локализация добавлена для следующих элементов:

- подсказки взаимодействия с исследователем;
- подсказки взаимодействия с бандитом;
- окна получения задания;
- окна переговоров с бандитом;
- подбора квестового предмета;
- текста текущей цели;
- экрана завершения уровня;
- экрана смерти / провала.

Для примера добавлена английская локализация `en`.

Файлы локализации размещены в стандартной структуре проекта:

- `Content/Localization/Game/en/Game.po`;
- `Content/Localization/Game/en/Game.locres`.

В `DefaultGame.ini` добавлен путь к игровым localization resources, чтобы переводы загружались при запуске проекта.

Для проверки локализации во время игры добавлено переключение языка.

В `AUE_LearningPlayerController` добавлены функции:

- `SetGameLanguage`;
- `PrintGameLanguage`.

Смена языка подключена через существующую систему Enhanced Input.

Добавлен input tag:

- `Input.ToggleLanguage`.

Переключение языка назначено на клавишу `L`.

После смены языка обновляется текущая подсказка взаимодействия, чтобы текст над объектом сразу отображался на выбранном языке.

В систему целей добавлена обработка части данных через `AsyncTask`.

При запуске цели `TPLevelObjectiveManager` сначала собирает snapshot состояния NPC в игровом потоке.

После этого на background-потоке выполняется:

- подсчёт общего количества целей;
- подсчёт живых целей;
- выбор NPC, с которого может выпасть квестовый предмет.

Результат возвращается обратно в `GameThread`, где безопасно обновляются `UObject`, привязываются делегаты смерти NPC и создаётся UI цели.

Для проверки работы многопоточности добавлены диагностические сообщения в `Output Log` с фильтром:

`AsyncObjective`

По этим сообщениям видно, что расчёт выполняется не в игровом потоке, а применение результата возвращается обратно в `GameThread`.

В рамках домашней работы добавлены и доработаны:

- локализуемые тексты в квестовых компонентах;
- английская локализация `en`;
- переключение языка через Enhanced Input;
- `TPMessageScreenWidget`;
- асинхронная обработка старта цели в `TPLevelObjectiveManager`;
- диагностические логи для проверки многопоточности.


---

## Проектная работа

После выполнения домашних работ проект был продолжен как единое игровое приложение на основе уже созданной архитектуры персонажей, взаимодействия, NPC, квестов, UI, оружия, аудио, VFX и локализации.

Основная цель этапа — собрать проектную карту с полноценным игровым циклом: главное меню, выбор уровня, квестодатель, несколько квестовых предметов, враждебные NPC, экран завершения, экран смерти и меню паузы.

### Главное меню и выбор уровня

Добавлен отдельный уровень главного меню.

Для главного меню создан отдельный UI-экран `WBP_ProjectMainMenu`, который используется как стартовая точка проекта.

В меню добавлены:

- кнопка запуска проектной карты;
- кнопка выбора уровня;
- кнопка выхода из игры;
- отдельная панель выбора уровня;
- переходы на проектную карту, карту с большинством домашних заданий и карту домашней работы по Landscape.

Кнопки главного меню оформлены в едином sci-fi стиле проекта: тёмные полупрозрачные панели, золотые акценты, подсветка при наведении и нажатии.

Фон главного меню вынесен в отдельную UI-текстуру и подключён через `Image` в `WBP_ProjectMainMenu`. Это позволяет менять визуальное оформление меню без изменения логики переходов между уровнями.

Для главного меню добавлен C++ класс `TPMainMenuWidget`. Тексты кнопок заполняются из C++ через `FText`, а текстовые поля в Widget Blueprint остаются пустыми. Такой подход сохраняет совместимость с последующей локализацией.

### Экран завершения задания и экран смерти

Для экрана завершения задания тексты хранятся в `TPLevelObjectiveManager`:

- заголовок завершения;
- текст кнопки рестарта;
- текст перехода в главное меню;
- текст выхода.

Для экрана смерти тексты хранятся в `TPPlayerCharacter`:

- заголовок смерти;
- текст кнопки рестарта;
- текст перехода в главное меню;
- текст выхода.

В существующий Widget Blueprint добавлены дополнительные кнопки:

- `RestartButton`;
- `MainMenuButton`;
- `QuitButton`.

Имена текстовых блоков синхронизированы с C++-полями, чтобы `BindWidgetOptional` корректно находил элементы и заполнял текст во время выполнения.

Так экран смерти и экран успешного завершения используют одну общую архитектуру, но получают разные данные из разных игровых систем.

### Меню паузы

Добавлено игровое меню паузы, вызываемое во время игры клавишей `Esc`.

Для меню паузы создан C++ класс `TPPauseMenuWidget` и Blueprint-виджет `WBP_PauseMenu`.

Меню паузы содержит:

- кнопку перезапуска текущего уровня;
- кнопку возврата в главное меню;
- кнопку выхода из игры.

При открытии меню:

- игра ставится на паузу;
- включается курсор мыши;
- управление переключается в UI-only режим;
- слева отображается полупрозрачная панель с кнопками;
- основная игровая сцена остаётся видимой на фоне.

Для вызова меню добавлен новый input tag:

- `Input.PauseMenu`.

К нему подключён отдельный `Input Action`, назначенный на клавишу `Esc`.

Логика открытия и закрытия меню подключена к `TPPlayerCharacter`, чтобы меню работало на игровых картах и не зависело от конкретного Level Blueprint.

### Проектный квестодатель

Добавлен новый NPC-квестодатель — Хранитель.

Хранитель использует существующую систему NPC и взаимодействия:

- базовый класс `TPNPCCharacter`;
- компонент квестодателя `TPQuestGiverComponent`;
- систему подсказок взаимодействия;
- `TPLevelObjectiveManager` для управления целью уровня.

Для Хранителя настроен отдельный Blueprint `BP_NPC_Guardian`.

В отличие от старого исследователя из домашних заданий, Хранитель используется на проектной карте и выдаёт проектное задание, связанное с двумя предметами.

### Расширение квестовой цели на несколько предметов

Квестовая система доработана так, чтобы цель могла требовать не один предмет, а несколько разных квестовых предметов.

Для проектного задания используются два предмета:

- древний инструмент;
- контейнер с биоматериалом.

Древний инструмент получается после победы над командиром захватчиков.

Контейнер с биоматериалом размещается на проектной карте вручную как отдельный квестовый объект.

`TPLevelObjectiveManager` теперь может отслеживать прогресс сбора нескольких предметов и обновлять UI цели по общему количеству собранных объектов.

Старая логика одного квестового предмета сохранена для совместимости с картами домашних заданий.

### Квестовые предметы и outline

Для проектной карты добавлены отдельные квестовые предметы:

- `BP_QuestItem_AncientTool`;
- `BP_QuestItem_BiomaterialContainer`.

Предметы используют уже существующую систему взаимодействия и подсказок.

Для квестовых предметов включён outline через `CustomStencil` и ранее созданный post-process материал обводки.

Для предметов добавлен режим постоянной подсветки, чтобы важные объекты можно было заметить без обязательного наведения прицела.

Контейнер с биоматериалом размещается на уровне вручную и связывается с `TPLevelObjectiveManager` через instance-ссылку на карте.

Древний инструмент создаётся через логику выпадения после смерти нужного NPC.

### Командир и враги проектной карты

Для проектной карты добавлен отдельный командир захватчиков.

Командир отличается от обычных врагов:

- не респавнится;
- используется как уникальная цель задания;
- имеет отдельные параметры здоровья;
- является источником выпадения древнего инструмента.

Обычные враги размещаются через систему точек респавна.

Командир добавляется в `TargetNPCs` менеджера цели уровня вручную, чтобы его смерть учитывалась отдельно от обычных респавнящихся NPC.

### Неуязвимость квестодателя

В базовый класс персонажа `TPBaseCharacter` добавлена настройка получения gameplay-урона.

Добавлен флаг:

- `bCanReceiveGameplayDamage`.

Через него можно отключить получение урона для отдельных персонажей, не удаляя у них Ability System Component и не ломая общую иерархию.

В `TPAttributeSet` проверяется, может ли персонаж получать gameplay-урон. Если урон отключён, значение `Damage` сбрасывается, но здоровье не уменьшается и смерть не вызывается.

Для `BP_NPC_Guardian` отключено получение урона, поэтому квестодатель не может быть случайно убит игроком или врагами.

### Отключение health bar для friendly / quest NPC

В `TPNPCCharacter` добавлена настройка отображения полоски здоровья.

Добавлен флаг:

- `bShowHealthBar`.

Если флаг выключен, `HealthBarWidgetComponent` скрывается и не инициализируется.

Для Хранителя health bar отключён, чтобы friendly / quest NPC не выглядел как цель для атаки.

У обычных врагов и командира health bar остаётся включённым.

### Доработка анимации врагов проектной карты

Для врагов проектной карты подключён и адаптирован Animation Blueprint из набора Invader / Wraith.

Вместо создания отдельной анимационной системы для врагов используется уже существующая архитектура проекта:

- `TPCharacterAnimInstance` остаётся базовым источником общих анимационных данных;
- `ABP_Invader` использует данные скорости и состояния движения;
- переменные Fab Animation Blueprint синхронизируются с проектными переменными через отдельную ветку адаптации в Event Graph.

В `ABP_Invader` добавлена передача данных:

- `Speed_0` получает скорость из общей переменной `Speed`;
- `IsAccelerating` рассчитывается через порог скорости;
- `IsInAir` получает значение из общей переменной падения.

Это позволило использовать существующий State Machine ассета без переписывания всей логики под NPC проекта.

Для движения врагов добавлено отдельное состояние ходьбы:

- `Walk` в State Machine `Ground Locomotion`;
- переходы `Idle → Walk`, `Walk → Idle`, `Walk → Run`, `Run → Walk`;
- пороги скорости, разделяющие состояние покоя, ходьбы и бега.

Скорость патрулирования и преследования по-прежнему задаётся через `TPNPCDefinition`:

- `WalkSpeed` — обычное движение / патрулирование;
- `ChaseSpeed` — преследование игрока.

Так анимация врагов остаётся связанной с уже существующими Data Asset настройками NPC, а не с жёстко заданными значениями в Animation Blueprint.

### Видимость health bar и outline через препятствия

Для project polish доработана видимость UI и подсветки объектов через препятствия.

В `TPNPCCharacter` добавлена проверка видимости health bar от камеры игрока. Полоска здоровья скрывается, когда между камерой и NPC находится объект, блокирующий канал `Visibility`.

Добавлены параметры:

- `bHideHealthBarWhenOccluded`;
- `HealthBarVisibilityCheckInterval`.

Проверка выполняется периодически через timer, а не каждый кадр. Это уменьшает лишнюю нагрузку и сохраняет предсказуемое поведение UI.

В `AInteractableActor` добавлена аналогичная проверка для outline через `CustomDepth / CustomStencil`.

Добавлены параметры:

- `bHideHighlightWhenOccluded`;
- `HighlightVisibilityCheckInterval`.

Квестовые предметы продолжают использовать режим постоянной подсветки, но подсветка теперь отображается только при прямой видимости от камеры игрока. Это сохраняет читаемость важных предметов и не позволяет видеть их через стены.

Так как `TPQuestItemActor` наследуется от `AInteractableActor`, новая логика применяется к проектным предметам без дублирования кода.

### Уточнение логики попаданий в голову

В `TPBlasterProjectile` доработана логика попаданий в голову.

Раньше проверка headshot применялась ко всем персонажам, включая игрока. Теперь правила headshot применяются только к враждебным NPC.

Добавлены:

- `HeadshotDamageMultiplier`;
- проверка применимости headshot-правил к Actor;
- разделение обычного попадания и попадания в голову по вражескому NPC.

При попадании по голове враждебного NPC урон умножается на заданный коэффициент.

### Очистка отладочных сообщений

После настройки AI, стрельбы, восприятия целей и headshot часть диагностических сообщений была вынесена за отдельные debug-флаги.

Для projectile добавлен параметр:

- `bLogProjectileDebug`.

Для AI Controller добавлен параметр:

- `bLogAIDebug`.

В обычном режиме эти флаги выключены, поэтому `Output Log` не заполняется сообщениями о восприятии целей, смене цели, потере цели, попаданиях projectile и проверке Ability System Component.

При необходимости отладки флаги можно включить точечно в соответствующих Blueprint / C++ настройках, не возвращая постоянный спам логов в основную игру.

### Локализация

Система локализации, добавленная в домашней работе 8, сохранена и расширена.

Недостающие английские переводы были добавлены для новых UI-строк:

- главное меню;
- выход;
- прогресс сбора нескольких предметов;
- завершение сбора предметов;
- кнопки экрана смерти;
- кнопки экрана завершения задания.

Финальная синхронизация локализации оставлена на завершающий этап проекта, чтобы собрать все новые строки одним проходом через Localization Dashboard.

### Сохранение совместимости с домашними заданиями

Все изменения добавлялись как развитие общей архитектуры проекта.

Старые карты домашних заданий не удалялись и не переписывались.

Для новых механик использованы отдельные Blueprint-ассеты, отдельные настройки Data Asset и instance-ссылки на проектной карте.

Существующие классы и компоненты расширены через параметры, а не через дублирование логики:

- `TPBaseCharacter` — общий флаг получения урона;
- `TPNPCCharacter` — настройка отображения health bar и проверка видимости полоски здоровья через препятствия;
- `AInteractableActor` — проверка видимости outline через препятствия;
- `TPQuestItemActor` — использование общей логики взаимодействия и подсветки для проектных предметов;
- `TPLevelObjectiveManager` — расширение цели на несколько предметов;
- `TPBlasterProjectile` — уточнение правил headshot и отключаемые projectile debug-логи;
- `TPNPCAIController` — отключаемые AI debug-логи и управление восприятием целей;
- `TPMessageScreenWidget` — общий экран для завершения и смерти;
- `TPPlayerCharacter` — подключение меню паузы;
- `TPMainMenuWidget` — отдельная логика главного меню;
- `TPPauseMenuWidget` — отдельная логика меню паузы;
- `TPCharacterAnimInstance` — общие данные движения, используемые игроком и адаптированные для Animation Blueprint врагов.